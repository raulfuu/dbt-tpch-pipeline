name: dbt CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  cd-dbt:
    runs-on: ubuntu-latest
    
    env:
      DBT_SNOWFLAKE_ACCOUNT: ${{ secrets.DBT_SNOWFLAKE_ACCOUNT }}
      DBT_SNOWFLAKE_USER: ${{ secrets.DBT_SNOWFLAKE_USER }}
      DBT_SNOWFLAKE_PASSWORD: ${{ secrets.DBT_SNOWFLAKE_PASSWORD }}
      DBT_SNOWFLAKE_ROLE: ${{ secrets.DBT_SNOWFLAKE_ROLE }}
      DBT_SNOWFLAKE_WAREHOUSE: ${{ secrets.DBT_SNOWFLAKE_WAREHOUSE }}
      DBT_SNOWFLAKE_DATABASE: ${{ secrets.DBT_SNOWFLAKE_DATABASE }}
      DBT_SNOWFLAKE_SCHEMA: ${{ secrets.DBT_SNOWFLAKE_SCHEMA }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dbt-snowflake
        run: pip install dbt-snowflake

      - name: Install dbt Packages
        run: dbt deps

      - name: Full Production Run
        run: dbt run --select tpch_star_schema --profiles-dir .

      - name: Generate Documentation
        run: dbt docs generate --profiles-dir .

      - name: Serve Documentation
        run: |
          dbt docs serve --port 8080 &          
          SERVER_PID=$!
          
          # Sleep, ping and kill to finish pipeline
          sleep 5
          curl -I http://localhost:8080          
          kill $SERVER_PID